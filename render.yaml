services:
  - type: worker
    name: discord-bot
    runtime: python
    buildCommand: |
      curl -fsSL https://deno.land/install.sh | sh
      pip install -r requirements.txt
    startCommand: python mybot.py
